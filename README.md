Password Manager

A simple, local password manager built with Tkinter.
It generates secure random passwords and automatically copies them to your clipboard.
Saved logins (website, email/username, and password) are stored in a local text file on your machine.